
{
  "name": "aspire-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && test \"$(git branch --show-current)\" = main && test -z \"$(git status --porcelain)\" && wrangler pages deploy . --project-name aspire-pages"
  }
}