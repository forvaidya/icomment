{
  "name": "aspire-pages",
  "private": true,
  "scripts": {
    "deploy": "test \"$(git branch --show-current)\" = main && test -z \"$(git status --porcelain)\" && wrangler pages deploy . --project-name aspire-pages"
  }
}